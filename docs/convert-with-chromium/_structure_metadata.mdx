import ApiEndpoint from "@site/src/components/documentation/ApiEndpoint";

## Structure & Metadata

<ApiEndpoint
  formFields={[
    {
      name: "generateDocumentOutline",
      type: "boolean",
      defaultValue: "false",
      description:
        "Chromium feature. Embeds the document outline (bookmarks) into the PDF.",
    },
    {
      name: "metadata",
      type: "json",
      defaultValue: "None",
      description: "Writes metadata (Author, Title, etc.).",
    },
    {
      name: "flatten",
      type: "boolean",
      defaultValue: "false",
      description:
        "Converts form fields into static content, preventing further editing.",
    },
  ]}
  formFiles={[
    {
      name: "embeds",
      type: "file[]",
      defaultValue: "None",
      description: "Embeds files into the PDF.",
    },
  ]}
/>

### Document Outline (Chromium)

Use the `generateDocumentOutline` form field to automatically create a PDF bookmark pane (sidebar) based on your HTML headings (`<h1>` to `<h6>`).

- **Process:** Happens _during_ the HTML-to-PDF conversion.
- **Requirement:** Your HTML must use proper heading tags to generate the hierarchy.

<ApiEndpoint
  curl={`
curl \\
--request ${props.method} http://localhost:3000${props.path} \\
${props.curlFormData}
--form generateDocumentOutline=true \\
${props.curlOutput}
`}
/>

### Metadata (PDF Engines)

Use the `metadata` form field to inject XMP metadata into the generated PDF. This allows you to set properties like Author, Title, Copyright, and Keywords.

This field accepts a **JSON-formatted object**.

:::info Valid Keys
Not all metadata tags are writable. Gotenberg relies on ExifTool for this operation.
[See the XMP Tag Name documentation](https://exiftool.org/TagNames/XMP.html#pdf) for a list of potential keys.
:::

:::caution Compliance Warning
Writing metadata involves modifying the PDF structure and usually breaks **PDF/A** compliance.
:::

<ApiEndpoint
  curl={`
curl \\
--request ${props.method} http://localhost:3000${props.path} \\
${props.curlFormData}
--form 'metadata={"Author":"Julien Neuhart","Copyright":"Julien Neuhart","CreationDate":"2006-09-18T16:27:50-04:00","Creator":"Gotenberg","Keywords":["first","second"],"Marked":true,"ModDate":"2006-09-18T16:27:50-04:00","PDFVersion":1.7,"Producer":"Gotenberg","Subject":"Sample","Title":"Sample","Trapped":"Unknown"}' \\
${props.curlOutput}
`}
/>

### Attachments (PDF Engines)

Use the `embeds` form field to attach external files directly inside the PDF container.

**Common Use Case:**
This is essential for e-invoicing standards like **[ZUGFeRD / Factur-X](https://fnfe-mpe.org/factur-x/)**, which require a human-readable PDF to carry a machine-readable XML invoice as an attachment.

<ApiEndpoint
  curl={`
curl \\
--request ${props.method} http://localhost:3000${props.path} \\
${props.curlFormData}
--form embeds=@invoice.xml \\
--form embeds=@logo.png \\
${props.curlOutput}
`}
/>

### Flatten (PDF Engines)

Use the `flatten` form field to make the PDF non-interactive.

This process merges all interactive form fields (text inputs, checkboxes, etc.) directly into the page content. The resulting PDF cannot be modified by the end-user.

<ApiEndpoint
  curl={`
curl \\
--request ${props.method} http://localhost:3000${props.path} \\
${props.curlFormData}
--form flatten=true \\
${props.curlOutput}
`}
/>
