---
id: screenshot-url
title: Screenshot URL
---

import ConfigurationInfo from "./_configuration_info.mdx";
import ApiEndpoint from "@site/src/components/documentation/ApiEndpoint";
import RenderingBehaviorScreenshot from "./_rendering_behavior/_rendering_behavior_screenshot.mdx";
import HttpNetworking from "./_http_networking.mdx";
import Console from "./_console.mdx";
import Sponsors from "@site/src/components/documentation/Sponsors";

Converts a target URL into an image using Headless Chromium.

This route works by simulating a standard browser navigation: it connects to the URL, executes the necessary JavaScript, loads assets (CSS, images, fonts), and captures the final state of the DOM as a PDF. It supports Single Page Applications (SPAs) and dynamic content rendering.

<ConfigurationInfo />

## Basics

<ApiEndpoint
  method="POST"
  path="/forms/chromium/screenshot/url"
  headers={[
    {
      name: "Gotenberg-Output-Filename",
      type: "string",
      required: false,
      description:
        "The filename of the resulting file - Gotenberg automatically appends the file extension. Defaults to a random UUID filename.",
    },
    {
      name: "Gotenberg-Trace",
      type: "string",
      required: false,
      description:
        "A custom request ID to identify the request in the logs; overrides the default UUID.",
    },
  ]}
  formFields={[
    {
      name: "url",
      type: "string",
      required: true,
      description: "URL of the page to convert into PDF.",
    },
  ]}
  curl={`
curl \\
--request POST http://localhost:3000/forms/chromium/screenshot/url \\
--form url=https://my.url \\
-o my.jpeg`}
  responses={[
    {
      status: 200,
      message: "OK",
      description: "The screenshot has been successfully created.",
      body: `
Content-Disposition: attachment; filename={output-filename.ext}
Content-Type: {content-type}
Content-Length: {content-length}
Gotenberg-Trace: {trace}
Body: {output-file}
      `,
    },
    {
      status: 400,
      message: "Bad Request",
      description: "The request had one ore more invalid form fields.",
      body: `
Content-Type: text/plain; charset=UTF-8
Gotenberg-Trace: {trace}
Body: {error}
      `,
    },
    {
      status: "403",
      message: "Forbidden",
      description: "The URL is forbidden.",
      body: `
Content-Type: text/plain; charset=UTF-8
Gotenberg-Trace: {trace}
Body: Forbidden
      `,
    },
    {
      status: 503,
      message: "Service Unavailable",
      description:
        "The request did not complete within the configured maximum duration.",
      body: `
Content-Type: text/plain; charset=UTF-8
Gotenberg-Trace: {trace}
Body: Service Unavailable
      `,
    },
  ]}
/>

<RenderingBehaviorScreenshot
  method="POST"
  path="/forms/chromium/screenshot/url"
  curlFormData={`--form url=https://my.url \\`}
  curlOutput={`-o my.jpeg`}
/>

<HttpNetworking
  method="POST"
  path="/forms/chromium/screenshot/url"
  curlFormData={`--form url=https://my.url \\`}
  curlOutput={`-o my.jpeg`}
/>

<Console
  method="POST"
  path="/forms/chromium/screenshot/url"
  curlFormData={`--form url=https://my.url \\`}
  curlOutput={`-o my.jpeg`}
/>

<Sponsors />
