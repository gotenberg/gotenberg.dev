---
id: prometheus-metrics
title: Prometheus Metrics
---

import ApiEndpoint from "@site/src/components/documentation/ApiEndpoint";
import Sponsors from "@site/src/components/documentation/Sponsors";

Exposes internal metrics in [Prometheus](https://prometheus.io/) format. See the [Prometheus module configuration](/docs/configuration#prometheus) for details.

<ApiEndpoint
  method="GET"
  path="/prometheus/metrics"
  headers={[
    {
      name: "Gotenberg-Trace",
      type: "string",
      required: false,
      description:
        "A custom request ID to identify the request in the logs; overrides the default UUID.",
    },
  ]}
  curl={`curl --request GET http://localhost:3000/prometheus/metrics`}
  responses={[
    {
      status: 200,
      message: "OK",
      description: (
        <span>
          See{" "}
          <a href="https://demo.gotenberg.dev/prometheus/metrics">
            https://demo.gotenberg.dev/prometheus/metrics
          </a>{" "}
          for details.
        </span>
      ),
    },
  ]}
/>

<Sponsors />
